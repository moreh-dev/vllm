# Custom self-hosted runner labels so actionlint doesn't flag them as unknown
# in `runs-on`.
self-hosted-runner:
  labels:
    - vllm-runners
    # moreh internal runner pool (access to the 192.168.212.30 registry)
    - github-runner
